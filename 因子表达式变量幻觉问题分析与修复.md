# 因子表达式变量幻觉问题（`$return` Bug）分析与修复

> 日期：2026-07-16 · 模块：`quantaalpha/factors/coder/` · 故障现象：因子代码生成循环浪费大量 LLM 调用

---

## 1. 问题现象

因子挖掘的 debug 循环中，LLM 生成的因子表达式包含**数据中不存在的列名变量**，导致反复报错：

```
factor_expression: RANK(SIGN($returns) * TS_MEAN($volume / (TS_STD($volume, 5) + 1e-8), 5) * ...)
                   ^^^^^^^^
                   数据表里没有这列
```

典型错误信息：
- **`"The expression contains unresolved '$' prefixes ... causing a SyntaxError"`**（pyparsing 报错）
- LLM 第一轮写 `$return`（不存在），被纠正后第二轮改写成 `$returns`、`$return_rate`、`$avg_price` 等**变体**

一轮因子 debug 循环通常只能产出 1/3 ~ 2/3 通过，剩余全部浪费在"试错 hallucination 变量"上。

---

## 2. 根因定位

### 2.1 数据契约

Qlib A 股 `cn_data` 的**真实可用列（基特征）只有**：

```
$open, $high, $low, $close, $volume, $vwap
```

> 注意：**没有 `$return`**。收益率必须由 `TS_PCTCHANGE($close, 1)` 现场算出来。

### 2.2 出错位置

`quantaalpha/factors/coder/expr_parser.py` 中的 `parse_symbol`：

```python
def parse_symbol(expr, columns):
    replace_map = {}
    for col in columns:
        replace_map.update({col: col.replace('$', '')})   # 只处理「存在的列」
    for var, var_df in replace_map.items():
        expr = expr.replace(var, var_df)
    return expr
```

- 输入表达式含 `$return`，但 `$return` 不在 `columns` 里
- `replace_map` 里没有 `$return` 的条目 → 原样保留
- 把带 `$` 的表达式丢给 pyparsing → 得到**看不懂的** `unresolved '$' prefixes` 错误

### 2.3 为什么 prompt 约束治标不治本

最初尝试：在 `prompts.yaml` 系统提示里加一句
> "Never hallucinate variables (e.g. `$return` is NOT a column)"

**失败**。原因是**负向举例式 prompt** 堵不住 LLM 的变体：

| 轮次 | LLM 写的变量 | 结果 |
|---|---|---|
| 1 | `$return` | ❌（prompt 举例堵了这个）|
| 2 | `$returns` | ❌（变体，prompt 没堵到）|
| 3 | `$return_rate` | ❌（变体）|
| 4 | `$avg_price` 等 | ❌（变体无穷无尽）|

LLM 生成 `$xxx` 变量名的组合是**无限的**，靠 prompt 枚举永远堵不严。

---

## 3. 原项目（rdagent / 官方 QuantaAlpha）的方案

### 3.1 原项目架构

翻查 `rdagent` 包源码（`rdagent/components/coder/factor_coder/`），原项目**不存在 `$return` bug**，因为架构完全不同：

| 维度 | 原项目 rdagent | 本项目 QuantaAlpha |
|---|---|---|
| LLM 产物形态 | **完整的 `factor.py` 真 Python 文件**（实现 `calc_factor_by_expression` 函数，直接写 `df['$close']`） | **表达式字符串**（`RANK($open/($close+1e-8))`），经 pyparsing 转 Python |
| 变量校验层 | **没有单独的表达式校验层** | 靠 `parse_symbol` 字符串替换 + pyparsing |
| 写错变量时的报错 | 真代码直接抛 **`KeyError: '$return'`**，Traceback 清晰 | pyparsing 抛 **`"unresolved '$' prefixes"`**，语义模糊 |

### 3.2 原项目如何处理变量错误

原项目的 `FactorCodeEvaluator.evaluate` 流程（抽象如下）：

```
1. LLM 生成 factor.py 真代码
2. 在沙箱里 exec / run factor.py
3. 若报错 → 把完整 Traceback 塞进 feedback prompt
4. LLM 收到 feedback（包含具体错误 + 代码位置），下一轮修改
```

关键设计差异：
- **执行的是真 Python**，错误信息精确（`KeyError: '$return'`、`NameError`、`SyntaxError` 都有明确点位）
- **feedback 是自动机生成**，不是人工写约束被 LLM 猜
- **不依赖 prompt 正面/负面举例**，靠"报错 → 反馈 → 修正"的闭环

### 3.3 为什么本项目不能直接照搬

QuantaAlpha 把"真代码生成"改成了"表达式 + pyparsing 解析"，**原因是为了支持因子的变异（mutation）和交叉（crossover）**——表达式比真代码更适合做 AST 层面的遗传操作。

这个架构决定了本项目**必须保留表达式解析层**。而在表达式层里，一旦变量名没被替换就丢给 pyparsing，报错就是晦涩的 `unresolved '$'` 而非 `KeyError`，debug 循环靠 LLM 猜，效率低。

> **结论：原项目的"真代码 + 清晰报错"方案不可直接移植，因为本项目主动选择了表达式架构。需要在表达式层补一个轻量校验。**

---

## 4. 最终解决方案

### 4.1 设计原则

对齐原项目精神（**明确报错 → 精准反馈 → LLM 一次修对**），但适配本项目的表达式架构：
- 保留表达式 + pyparse 层（不动大架构）
- 在 `parse_symbol` 里加一行**未知变量静态校验**
- 报错要**同时列出未知变量 + 可用变量**，让 LLM 一次纠对

### 4.2 实现位置

`quantaalpha/factors/coder/expr_parser.py` 的 `parse_symbol` 函数

### 4.3 校验逻辑（伪代码）

```python
import re

def parse_symbol(expr, columns):
    # ---------- 新增：未知变量早失败 ----------
    # 扫描表达式里所有的 $xxx
    used_vars = set(re.findall(r'\$[A-Za-z_]\w*', expr))
    available = {c for c in columns if c.startswith('$')}
    unknown = sorted(used_vars - available)

    if unknown:
        # 对齐原项目"明确报错 + 列出可用项"的思想
        raise ValueError(
            f" hallucinated variables {unknown}; "
            f"available base features: {sorted(available)}"
        )
    # ----------------------------------------

    # 既有逻辑不动
    replace_map = {}
    ...
```

### 4.4 为什么这个方案优于 prompt 约束

| 维度 | prompt 约束 | 静态校验 |
|---|---|---|
| 覆盖 | 只堵了举例列出的几个 | **堵死所有变体**（正则扫表达式）|
| LLM 负担 | LLM 要"理解并遵守" | **机器直接拒收**，LLM 收到精准列表 |
| 反馈质量 | "你错了，自己改" | "你多写了 `$returns`，只允许用 `[$open,$high,$low,$close,$volume,$vwap]`" |
| 稳定性 | LLM 可能忽略 prompt | **确定性**，100% 命中 |

### 4.5 与 debug 循环（max_loop）的配合

- 新方案下，hallucination 变量**在第一轮就被静态校验拦下**
- LLM 收到 feedback 后，下一轮就能直接写出正确写法（如 `TS_PCTCHANGE($close, 1)` 算收益率）
- 配合 `max_loop: 5`，单因子成功率达到最高性价比

---

## 5. 关联改动（已 commit）

| Commit | 文件 | 作用 |
|---|---|---|
| `f488feb` | `quantaalpha/utils/env.py` | 修复 pyarrow 子进程 OOM（并发问题⑤）|
| `e796674` | `quantaalpha/factors/coder/prompts.yaml` | 临时 prompt 约束（**待本方案落地后撤回**）|
| `585d2c5` | `quantaalpha/coder/costeer/config.py` | debug max_loop 10 → 5 |

> 本方案落地后，commit `e796674` 的 prompt 约束应**撤回**（已被静态校验完全取代，且 prompt 约束会干扰 LLM 发挥）。

---

## 6. 待办

- [ ] 落地静态校验到 `parse_symbol`（本文件 §4.3）
- [ ] 撤回 `prompts.yaml` 的 prompt 约束（revert `e796674` 相关改动）
- [ ] 验证：触发一轮因子挖掘，确认 hallucination 变量首轮即被拒、debug 循环次数下降
- [ ] 同步上游：如官方 rdagent 后续支持本表达式架构，复用本方案

---

## 附录 A：可用的基特征列表

```text
$open         开盘价
$high         最高价
$low          最低价
$close        收盘价
$volume       成交量
$vwap         加权平均价
```

> 注：`$return`、`$returns`、`$volatility`、`$turnover` 等均**不是**原始列，需要
> 用 `TS_*` 算子现场计算。

## 附录 B：关键源文件

| 文件 | 作用 |
|---|---|
| `quantaalpha/factors/coder/expr_parser.py` | 因子表达式解析与列名替换（parse_symbol 所在）|
| `quantaalpha/backtest/factor_calculator.py` | 回测时执行因子表达式的入口 |
| `quantaalpha/factors/coder/prompts.yaml` | 因子实现 / 评估 prompt 模板（本次撤回的约束在此）|
| `quantaalpha/coder/costeer/config.py` | CoSTEER 配置（max_loop 所在）|
| `quantaalpha/utils/env.py` | 本地执行环境（OMP 线程限制在此）|
| `quantaalpha/core/scenario.py` | 场景描述抽象（生成发给 LLM 的 prompt 上下文）|
